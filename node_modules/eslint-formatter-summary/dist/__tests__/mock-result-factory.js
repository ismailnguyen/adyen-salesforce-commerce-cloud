"use strict";

require("core-js/modules/es.array.map");

module.exports = function resultFactory(failingRules) {
  var messages = failingRules.map(function (rule) {
    var ruleId = rule[0];
    var severity = rule[1];
    return {
      ruleId,
      severity,
      message: `'${ruleId}' has failed`,
      line: 1,
      column: 1,
      nodeType: 'Identifier',
      source: "const foo = 'bar';"
    };
  });
  return {
    messages
  };
};