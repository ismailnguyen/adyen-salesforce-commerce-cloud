"use strict";

var _findRule = _interopRequireDefault(require("../find-rule"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

test('returns a rule if already included in summary', function () {
  var summary = [{
    ruleId: 'existing rule'
  }];
  expect((0, _findRule.default)(summary, 'existing rule')).toEqual({
    ruleId: 'existing rule'
  });
  expect((0, _findRule.default)(summary, 'non-existing rule')).toBeUndefined();
});