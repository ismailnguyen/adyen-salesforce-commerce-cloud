"use strict";

require("core-js/modules/es.array.map");

var _sortByProp = _interopRequireDefault(require("../sort-by-prop"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

test('sorts array by given property and direction', function () {
  var createArray = function createArray() {
    for (var _len = arguments.length, values = new Array(_len), _key = 0; _key < _len; _key++) {
      values[_key] = arguments[_key];
    }

    return values.map(function (value) {
      return {
        value
      };
    });
  };

  var array = createArray('c', 'b', 'a', 'b');
  (0, _sortByProp.default)('value', array, 'asc');
  expect(array).toEqual([{
    value: 'a'
  }, {
    value: 'b'
  }, {
    value: 'b'
  }, {
    value: 'c'
  }]);
  array = createArray('a', 'c', 'b');
  (0, _sortByProp.default)('value', array, 'desc');
  expect(array).toEqual([{
    value: 'c'
  }, {
    value: 'b'
  }, {
    value: 'a'
  }]);
});